## Pending Tasks

The following high-level framework items are tracked for future sessions:

1. **Consolidate prompt boilerplate** into reusable templates under `templates/`. *(Completed)*
2. **Split long prompt files** into smaller, composable standard modules. *(Completed)*
3. **Create a repository adoption guide** (`docs/user/adoption_guide.md`). *(Completed)*
4. **Create a reusable AGENTS.md template** (`templates/agents_template.md`). *(Completed)*
5. **Restore and improve the adapter layer** (`adaptors/agy/` & `docs/user/adapter_usage.md`). *(Completed)*
6. **Audit architecture** for maintainability and modularity (`planning/architecture_audit.md`). *(Completed)*
